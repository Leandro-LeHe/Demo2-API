CREATE TABLE USUARIOS (
                          id INT PRIMARY KEY,
                          nome VARCHAR(100),
                          email VARCHAR(100),
                          endereco VARCHAR(200)
);
